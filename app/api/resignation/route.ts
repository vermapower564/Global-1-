import { GET as getPlural, POST as postPlural, PATCH as patchPlural } from "../resignations/route";

export const dynamic = "force-dynamic";

export { getPlural as GET, postPlural as POST, patchPlural as PATCH };
